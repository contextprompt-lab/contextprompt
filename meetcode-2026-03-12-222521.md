# Execution Plan — Thursday, March 12, 2026 10:25 PM

> 3 task(s) from 1m meeting · 1 speaker(s)

## Decisions Made

1. Reduce text walls in the AI cards / donation detail view by simplifying or condensing displayed information
2. Clarify and clean up confusing date/metadata information shown inside AI cards
3. Investigate and fix the flickering header on Android when scrolling in the AI cards / donation detail area

## Execution Status

**Review before execution:** T1, T2
**Needs clarification:** T3

## Execution Order

T1 → T2 (after T1) → T3

---

| ID | Task | Status | Confidence | Files | Depends on |
|----|------|--------|------------|-------|------------|
| T1 | Simplify and reduce text density in AI analysis card views | review | medium | 10 file(s) | — |
| T2 | Clean up confusing date and metadata display in AI cards | review | medium | 6 file(s) | T1 |
| T3 | Fix flickering header on Android when scrolling in AI card detail screen | clarify | low | 5 file(s) | — |

---

### T1: Simplify and reduce text density in AI analysis card views

**Status:** review · **Confidence:** medium — Speaker clearly described 'text walls' inside what appears to be AI-related cards. The repo has AnalysisResultCard, InsightsCard, InsightsModal, RecommendationsCard, RecommendationsModal, HealthCheckModal, GutPredictionModal, and LifestyleTipsModal as likely candidates. The exact screen referred to as 'the AI cards / donation' is ambiguous — 'donation' may be ASR noise for 'detail' or another word. Without seeing the screens, we cannot know which cards are the worst offenders or what truncation/condensation strategy is appropriate.

**Why:** Speaker explicitly complained about text walls in the AI card area, saying a normal user would be confused by the volume and density of text.

**Proposed change:** Audit AI-result-facing UI components (AnalysisResultCard, InsightsCard, RecommendationsCard, HealthCheckModal, GutPredictionModal, LifestyleTipsModal, InsightsModal) for overly long text blocks. Introduce truncation with 'read more' expansion, reduce verbose copy, or collapse secondary detail sections behind a toggle.

**Evidence:**
> [00:19] Speaker 1: In the AI cards, when you go inside the donation, there are text walls. It's just not clear — if I'm a normal user and I go in there, I get really confused on what's going on.

**High-confidence files:**
- `src/components/ui/AnalysisResultCard.tsx` — Primary AI analysis result display component; most likely source of dense text blocks including detailedBreakdown fields.
- `src/components/ui/InsightsCard.tsx` — Displays AI-generated insights; likely renders multiple InsightItem entries that can produce text walls.
- `src/components/ui/RecommendationsCard.tsx` — Renders gut health and stool type suggestion strings from the AI; these are known to be verbose.

**Possible related files** (inferred, not directly referenced):
- `src/components/ui/InsightsModal.tsx` — Modal wrapper for insights; may render the same dense text in a scroll view.
- `src/components/ui/RecommendationsModal.tsx` — Modal wrapper for recommendations; same concern as RecommendationsCard.
- `src/components/ui/HealthCheckModal.tsx` — Renders health metric recap and subtitle strings per metric; can produce multiple text-heavy sections.
- `src/components/ui/GutPredictionModal.tsx` — Renders title and body from gut prediction response; body field may be long.
- `src/components/ui/LifestyleTipsModal.tsx` — Renders tip cards with body text; multiple tips displayed at once could create text walls.
- `src/screens/profile/ProfileScreen.tsx` — Likely the screen that hosts or triggers these AI cards; may need layout or spacing adjustments.
- `src/screens/profile/DetailsScreen.tsx` — Named 'DetailsScreen' — could be the screen speaker referred to when describing the detail/donation view with text walls.

**Steps:**

1. Review each of AnalysisResultCard.tsx, InsightsCard.tsx, and RecommendationsCard.tsx to identify the longest rendered text fields.
2. Check which fields from the AI response types (analysis, detailedBreakdown, gut_health_suggestions, stool_type_suggestions, body) are rendered inline without truncation.
3. For each field exceeding a reasonable line threshold (e.g., >3 lines), implement a collapsed/expandable pattern or a 'Read more' toggle.
4. Apply the same review to InsightsModal, RecommendationsModal, HealthCheckModal, GutPredictionModal, and LifestyleTipsModal.
5. Check DetailsScreen.tsx to determine if it is the screen the speaker was referring to and apply the same audit.
6. Confirm with the product owner which screen is the 'AI cards / detail' screen before finalizing changes.

**Ambiguities:**
- The word 'donation' is likely ASR noise — the intended word is unclear (could be 'detail', 'section', or a screen name).
- It is not specified which AI card type is the primary offender.
- No guidance given on preferred fix: truncation, collapsible sections, or rewriting copy.

**Task assumptions:**
- Assuming 'AI cards' refers to the AI analysis/insights/recommendations UI components in the profile or logPhoto flow.
- Assuming 'donation' is an ASR transcription error for another word describing a detail view.

---

### T2: Clean up confusing date and metadata display in AI cards

**Status:** review · **Confidence:** medium — Speaker specifically called out dates and 'confusing information' alongside the text walls in the same area. AI response types include generatedAt, periodStart, periodEnd, and similar date fields. It is not certain which components render these or how they are currently formatted/labeled. · **Depends on:** T1

**Why:** Speaker explicitly flagged that dates and other metadata shown in the AI card area are confusing and unclear to a normal user.

**Proposed change:** Audit AI card components for raw date strings (generatedAt, periodStart, periodEnd, createdDate) being rendered without user-friendly labels or formatting. Replace raw ISO strings with human-readable relative or formatted dates, or remove non-essential metadata from the default view.

**Evidence:**
> [00:19] Speaker 1: There's a lot of dates information, just confusing information — it's not very clear.

**High-confidence files:**
- `src/components/ui/AnalysisResultCard.tsx` — Renders AI analysis results including date-related metadata; primary candidate for raw date display.

**Possible related files** (inferred, not directly referenced):
- `src/components/ui/InsightsCard.tsx` — May display generatedAt or date range metadata from InsightsResponse.
- `src/components/ui/HealthCheckModal.tsx` — HealthCheckResponse includes generatedAt and dataPointsAnalyzed; these may be rendered visibly.
- `src/components/ui/GutPredictionModal.tsx` — GutPredictionResponse includes generatedAt and date fields per day entry.
- `src/utils/formatTimeAgo.ts` — Existing utility for human-readable date formatting; should be used or extended for any date rendering fixes.
- `src/screens/profile/DetailsScreen.tsx` — May be the screen rendering date metadata alongside AI results.

**Steps:**

1. Search AnalysisResultCard.tsx, InsightsCard.tsx, HealthCheckModal.tsx, and GutPredictionModal.tsx for any rendering of date strings (generatedAt, periodStart, periodEnd, createdDate, date fields).
2. For each found instance, determine if the date adds user value; if not, remove it from the default view.
3. If the date is useful context, replace raw ISO string rendering with formatTimeAgo() or a locale-formatted date string.
4. Check DetailsScreen.tsx for any additional date/metadata rendering.
5. Confirm with the product owner whether dates should be removed or reformatted.

**Ambiguities:**
- Not specified which exact dates are shown or which screen/component renders them.
- Unclear whether dates should be hidden entirely, reformatted, or better labeled.

**Task assumptions:**
- Assuming dates refer to fields like generatedAt, periodStart, periodEnd rendered as raw ISO strings.
- Assuming formatTimeAgo utility is available and appropriate for the fix.

---

### T3: Fix flickering header on Android when scrolling in AI card detail screen

**Status:** clarify · **Confidence:** low — Speaker mentioned the header flickers on Android when scrolling, but did not name the specific screen or component. The repo has many screens with headers. The AI card context suggests it is in the logPhoto or profile flow, but that is an inference. The root cause is also unknown — it could be an Animated header, a ScrollView/FlatList collision, or a React Navigation header issue.

**Why:** Speaker explicitly reported a header flickering bug on Android when scrolling in what appears to be the AI card / detail view.

**Proposed change:** Identify the screen where header flickering occurs on Android during scroll. Investigate whether the issue is caused by Animated header interpolation, ScrollView bounce behavior, or React Navigation header animation. Apply platform-specific fix.

**Evidence:**
> [00:47] Speaker 1: I was testing on Android. When I tried to scroll down and I don't have enough text to scroll down really — I can scroll a little bit, but not too much — the header flickers like crazy.

**Possible related files** (inferred, not directly referenced):
- `src/screens/profile/DetailsScreen.tsx` — Named 'DetailsScreen' and likely contains a scrollable view with a header; could be the AI detail view with the flickering issue.
- `src/screens/logPhoto/AIInfoScreen.tsx` — AI-specific screen in the logPhoto flow; may have a header that animates on scroll.
- `src/screens/profile/ProfileScreen.tsx` — Main profile/dashboard screen; may host AI cards in a scroll view with an animated header.
- `src/navigation/MainTabNavigator.tsx` — Defines tab navigation and header configuration; header behavior may be set globally here.
- `src/navigation/RootNavigator.tsx` — Root navigation config; may set screen-level header options affecting Android behavior.

**Steps:**

1. Reproduce the flickering header on an Android device or emulator by navigating to the AI card / detail screen and attempting to scroll.
2. Identify whether the header is a React Navigation header or a custom component rendered inside the screen.
3. If it is a React Navigation header, check navigation options for that screen in MainTabNavigator.tsx and RootNavigator.tsx for animated header config.
4. If it is a custom header, inspect DetailsScreen.tsx and AIInfoScreen.tsx for Animated.Value interpolations tied to scroll position.
5. Check if the issue is related to insufficient scroll height by testing with useNativeDriver: true on any Animated values.
6. Apply platform-specific fix (e.g., android_hyphenationFrequency, scrollEventThrottle tuning, or disabling header animation on Android).
7. Confirm with the product owner which screen triggers the bug before writing the fix.

**Ambiguities:**
- Exact screen where flickering occurs is not named.
- Unknown whether the header is a React Navigation header or a custom in-component header.
- Unknown whether the flickering is tied to scroll distance or scroll speed.
- Speaker noted there is 'not enough text to scroll much' — the bug may only appear with minimal content height.

**Task assumptions:**
- Assuming the flickering is on the same screen discussed in T1/T2 (AI cards area).
- Assuming this is an Android-specific rendering issue, not iOS.

---

## Incomplete Items

- **Speaker referenced 'the AI cards, when you go inside the donation' — the word 'donation' was never clarified and may refer to a specific screen name or flow that is not identifiable from the transcript alone.**
  > [00:19] Speaker 1: In the AI cards, when you go inside the donation, put your text walls in.
  _The target screen or context for 'donation' is unclear; ASR likely corrupted a key word. Without knowing the exact screen, file mapping for T1 and T2 remains uncertain._

## Assumptions

1. The app is React Native based on file structure, README, and component naming conventions.
2. The word 'donation' in the transcript is an ASR transcription error; the intended word likely refers to a detail or section view within the AI cards flow.
3. The AI cards referenced are the analysis/insights/recommendations UI components in the profile or logPhoto flow, not a separate feature called 'donations'.
4. Both repos (poopcheck and softai-backend) were provided for context; all UI tasks apply to the poopcheck React Native repo only.

## Agent Execution Blocks

> Copy-paste any block below directly into Claude Code.

<details>
<summary>T1: Simplify and reduce text density in AI analysis card views (review first — check ambiguities before running)</summary>

```
Goal: Audit AI-result-facing UI components (AnalysisResultCard, InsightsCard, RecommendationsCard, HealthCheckModal, GutPredictionModal, LifestyleTipsModal, InsightsModal) for overly long text blocks. Introduce truncation with 'read more' expansion, reduce verbose copy, or collapse secondary detail sections behind a toggle.

Files:
- src/components/ui/AnalysisResultCard.tsx (primary) — Primary AI analysis result display component; most likely source of dense text blocks including detailedBreakdown fields.
- src/components/ui/InsightsCard.tsx (primary) — Displays AI-generated insights; likely renders multiple InsightItem entries that can produce text walls.
- src/components/ui/RecommendationsCard.tsx (primary) — Renders gut health and stool type suggestion strings from the AI; these are known to be verbose.
- src/components/ui/InsightsModal.tsx (check) — Modal wrapper for insights; may render the same dense text in a scroll view.
- src/components/ui/RecommendationsModal.tsx (check) — Modal wrapper for recommendations; same concern as RecommendationsCard.
- src/components/ui/HealthCheckModal.tsx (check) — Renders health metric recap and subtitle strings per metric; can produce multiple text-heavy sections.
- src/components/ui/GutPredictionModal.tsx (check) — Renders title and body from gut prediction response; body field may be long.
- src/components/ui/LifestyleTipsModal.tsx (check) — Renders tip cards with body text; multiple tips displayed at once could create text walls.
- src/screens/profile/ProfileScreen.tsx (check) — Likely the screen that hosts or triggers these AI cards; may need layout or spacing adjustments.
- src/screens/profile/DetailsScreen.tsx (check) — Named 'DetailsScreen' — could be the screen speaker referred to when describing the detail/donation view with text walls.

Steps:
1. Review each of AnalysisResultCard.tsx, InsightsCard.tsx, and RecommendationsCard.tsx to identify the longest rendered text fields.
2. Check which fields from the AI response types (analysis, detailedBreakdown, gut_health_suggestions, stool_type_suggestions, body) are rendered inline without truncation.
3. For each field exceeding a reasonable line threshold (e.g., >3 lines), implement a collapsed/expandable pattern or a 'Read more' toggle.
4. Apply the same review to InsightsModal, RecommendationsModal, HealthCheckModal, GutPredictionModal, and LifestyleTipsModal.
5. Check DetailsScreen.tsx to determine if it is the screen the speaker was referring to and apply the same audit.
6. Confirm with the product owner which screen is the 'AI cards / detail' screen before finalizing changes.

Watch out for:
- The word 'donation' is likely ASR noise — the intended word is unclear (could be 'detail', 'section', or a screen name).
- It is not specified which AI card type is the primary offender.
- No guidance given on preferred fix: truncation, collapsible sections, or rewriting copy.

Assumptions made:
- Assuming 'AI cards' refers to the AI analysis/insights/recommendations UI components in the profile or logPhoto flow.
- Assuming 'donation' is an ASR transcription error for another word describing a detail view.
```
</details>

<details>
<summary>T2: Clean up confusing date and metadata display in AI cards (review first — check ambiguities before running)</summary>

```
Goal: Audit AI card components for raw date strings (generatedAt, periodStart, periodEnd, createdDate) being rendered without user-friendly labels or formatting. Replace raw ISO strings with human-readable relative or formatted dates, or remove non-essential metadata from the default view.

Files:
- src/components/ui/AnalysisResultCard.tsx (primary) — Renders AI analysis results including date-related metadata; primary candidate for raw date display.
- src/components/ui/InsightsCard.tsx (check) — May display generatedAt or date range metadata from InsightsResponse.
- src/components/ui/HealthCheckModal.tsx (check) — HealthCheckResponse includes generatedAt and dataPointsAnalyzed; these may be rendered visibly.
- src/components/ui/GutPredictionModal.tsx (check) — GutPredictionResponse includes generatedAt and date fields per day entry.
- src/utils/formatTimeAgo.ts (check) — Existing utility for human-readable date formatting; should be used or extended for any date rendering fixes.
- src/screens/profile/DetailsScreen.tsx (check) — May be the screen rendering date metadata alongside AI results.

Steps:
1. Search AnalysisResultCard.tsx, InsightsCard.tsx, HealthCheckModal.tsx, and GutPredictionModal.tsx for any rendering of date strings (generatedAt, periodStart, periodEnd, createdDate, date fields).
2. For each found instance, determine if the date adds user value; if not, remove it from the default view.
3. If the date is useful context, replace raw ISO string rendering with formatTimeAgo() or a locale-formatted date string.
4. Check DetailsScreen.tsx for any additional date/metadata rendering.
5. Confirm with the product owner whether dates should be removed or reformatted.

Watch out for:
- Not specified which exact dates are shown or which screen/component renders them.
- Unclear whether dates should be hidden entirely, reformatted, or better labeled.

Assumptions made:
- Assuming dates refer to fields like generatedAt, periodStart, periodEnd rendered as raw ISO strings.
- Assuming formatTimeAgo utility is available and appropriate for the fix.
```
</details>

## Transcript

<details>
<summary>Full meeting transcript (149 words, 1m)</summary>

```
[00:03] Speaker 1: So a big thing. Just. to going over the app review and everything.
[00:12] Speaker 1: So I really don't like there's a lot of text. walls within the app.
[00:19] Speaker 1: For example, in the I think it's the AI cards. When you go inside the donation, put your text walls in. then, also, like, it looks like there's a lot of. like, dates information just kinda. confusing information. It's not very clear. It's just not. like, if I'm a normal user and I go in there, I get really confused on what's going on.
[00:47] Speaker 1: So that's the. probably the main thing also, like, in the same same area. Like, I was testing on Android for now, but when I tried. to scroll down and, I don't have enough. text to scroll down really. Like, I can scroll a little bit, but not too much. Like, the header. flickers like crazy. Just FYI.
```

</details>
