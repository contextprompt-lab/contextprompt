# Meeting Tasks — Thursday, March 12, 2026 02:05 AM

> Generated by meetcode from a 2m meeting · 3 task(s) extracted

## Summary

The speaker identified three UI fixes needed for the PoopCheck app: (1) remove validation errors from the profile screen that shouldn't be showing, (2) add a close/X button to the AI chat modal which currently lacks one, and (3) increase the blur intensity on the background image shown during the photo analysis loading screen so the image is obscured but still faintly visible. A fourth item was mentioned but cut off before being fully described.

## Speakers

- **Speaker 1** (speaker_0)

---

## Tasks

| ID | Task | Priority | Files | Depends on |
|----|------|----------|-------|------------|
| T1 | Remove errors from Profile screen | high | 5 file(s) | — |
| T2 | Add X close button to AI Chat modal | high | 5 file(s) | — |
| T3 | Increase background image blur on AnalyzingScreen | medium | 1 file(s) | — |

---

### T1: Remove errors from Profile screen

**Priority:** high

> [00:05] Speaker 1: 'the profile, Should actually not have errors, but we should just sit in the, then we are in, there's no need for those errors.'

**Files:**
- `src/screens/profile/ProfileScreen.tsx`
- `src/screens/profile/EditAccountInfoScreen.tsx`
- `src/screens/profile/ProfileSettingsScreen.tsx`
- `src/screens/profile/EditAccountInfoFormModal.tsx`
- `src/hooks/useUserProfile.ts`

**Instructions:**

1. Open `src/screens/profile/ProfileScreen.tsx` and inspect the component for any error state displays, inline validation messages, or error text rendered in the UI.
2. Identify where error states are conditionally rendered (look for error variables, error boundary renders, or inline `{error && <Text>...</Text>}` patterns).
3. Also check `src/screens/profile/EditAccountInfoScreen.tsx`, `src/screens/profile/ProfileSettingsScreen.tsx`, and `src/screens/profile/EditAccountInfoFormModal.tsx` for similar error displays that surface on the profile section.
4. Remove or suppress the error UI elements — do not remove the underlying error handling logic, just hide the visible error messages from the user-facing profile screen.
5. If errors are coming from a hook such as `useUserProfile` (in `src/hooks/useUserProfile.ts`), ensure the returned `error` field is simply not rendered rather than removing the hook logic.
6. Verify no console errors or crashes occur after the change by tracing data flow from the hook to the screen.

---

### T2: Add X close button to AI Chat modal

**Priority:** high

> [00:38] Speaker 1: 'the AI chat. So opens as a model. but yeah, actually, it doesn't have an x button. To close. So it should be basically. The x button. Button. Like, the. that. all the. other model. have.'

**Files:**
- `src/screens/profile/ChatScreen.tsx`
- `src/screens/profile/ChatHistoryScreen.tsx`
- `src/components/icons/EditSnapIcons.tsx`
- `src/components/icons/CommonIcons.tsx`
- `src/components/ui/HealthCheckModal.tsx`

**Instructions:**

1. Open `src/screens/profile/ChatScreen.tsx` which renders the AI chat interface as a modal.
2. Inspect the component's header/top area to confirm the absence of a close button.
3. Locate the `onClose` prop (already part of `ChatScreenProps` based on the export: `function ChatScreen({ onClose, initialChatId }: ChatScreenProps)`) — the close handler already exists, it just needs a UI button.
4. In the header or top-right corner of the modal, add a touchable close button using the existing `XIcon` or `CloseIcon`. Prefer `CloseIcon` from `src/components/icons/EditSnapIcons.tsx` (already exports `function CloseIcon({ color = '#FFFFFF' }: Omit<IconProps, 'size'>)`) or `XIcon` from `src/components/icons/CommonIcons.tsx` (exports `function XIcon({ color = COLORS.text.dark }: ColorProps)`).
5. Wire the button's `onPress` to call the `onClose` prop.
6. Match the styling/positioning used by other modals in the app — look at `src/components/ui/HealthCheckModal.tsx`, `src/components/ui/InsightsModal.tsx`, or `src/components/ui/GutPredictionModal.tsx` for the close button pattern to ensure visual consistency.
7. Also check `src/screens/profile/ChatHistoryScreen.tsx` (which has `onClose` too) and add a matching close button if it is also missing one.

---

### T3: Increase background image blur on AnalyzingScreen

**Priority:** medium

> [01:14] Speaker 1: 'the low. that loads. like, the. loader based. and then. in the. background, there's the. image. Dash. should be a lot more. blurred. So that. the. user can. now release. the image, but. like, they can. see it, but. not. release it. So. just. blur it up. A little more.'

**Files:**
- `src/screens/logPhoto/AnalyzingScreen.tsx`

**Instructions:**

1. Open `src/screens/logPhoto/AnalyzingScreen.tsx`. This screen receives a `photoUri` prop and displays the photo as a background while showing a loading/analyzing state.
2. Find where the photo background is rendered — look for an `Image` component using `photoUri`, likely wrapped or overlaid with a blur effect.
3. If using React Native's `<ImageBackground>` with a `blurRadius` prop, increase the `blurRadius` value significantly. A value in the range of 20–40 is typical for 'visible but obscured'; increase to at least 30–50 depending on the current value.
4. If using `@react-native-community/blur` or a similar library, increase the `blurAmount` or `blurRadius` property on the `BlurView` component wrapping the image.
5. If no blur is currently applied, wrap the `Image`/`ImageBackground` with a `BlurView` or add `blurRadius` prop directly to the `ImageBackground`.
6. The goal per the speaker: the user should be able to tell there is an image present but should not be able to clearly make out its content. Test visually to confirm the image is noticeably blurred but still perceptible.

---

## Transcript

<details>
<summary>Full meeting transcript (162 words, 2m)</summary>

```
[00:05] Speaker 1: So. few things. to. fix. Are. are. the. I think, think, the. profile, Should. actually. not have. errors, but. we should. just sit. In the. the. then. we are. in,
[00:31] Speaker 1: there's no need. for those. errors. The other. thing. the. the
[00:38] Speaker 1: the. AI chat. The. So. opens as a. model. but. yeah, actually, it doesn't have an. x button. To close. So it should be. basically. The x button. Button.
[00:55] Speaker 1: Like, the. that. all the. other model. have.
[01:03] Speaker 1: Also,
[01:06] Speaker 1: you know. where. we're. you take a. picture. you go to the. analysis.
[01:14] Speaker 1: And then the. the low. that loads. like, the. loader based. and then. in the. background, there's the. image. Dash. should be
[01:27] Speaker 1: a lot. know. Let
[01:30] Speaker 1: Sorry. A lot more. blurred. So that. the. user can. now release. the image, but. like, they can. see it, but. not. release it. So. just. blur it up. A little more.
[01:46] Speaker 1: In. lastly, for. the
[01:54] Speaker 1: which we. have to define. Exact. but
[01:59] Speaker 1: for
```

</details>
